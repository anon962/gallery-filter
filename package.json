{
    "devDependencies": {
        "npm-watch": "^0.12.0",
        "typescript": "^5.4.5"
    },
    "scripts": {
        "build": "tsc --build tsconfig.json && node tools/append-header.js",
        "watch": "npm-watch"
    },
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "extensions": "ts,js"
        }
    }
}
